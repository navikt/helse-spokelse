name: Aiven config for de andre topicene våre

on:
  push:
    branches:
      - main
    paths:
      - 'deploy/boo-dev.yml'
      - 'deploy/boo-prod.yml'

jobs:
  deployDev:
    name: apply dev-gcp
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: nais/deploy/actions/deploy@v2
        env:
          CLUSTER: dev-gcp
          RESOURCE: deploy/boo-dev.yml
